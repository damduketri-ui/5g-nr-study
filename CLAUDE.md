# 5G NR 통신 이해 — 작업 규칙

이 저장소는 3GPP 5G NR 규격을 **직관적으로 이해하기 위한 학습 자료 모음**이다.
읽는 사람은 통신 전공자가 아닐 수도 있다. 규격서를 대체하는 것이 목표가 아니라,
규격서를 읽을 수 있는 몸을 만드는 것이 목표다.

---

## 1. 사실 정확성 — 가장 중요한 규칙

통신 규격은 그럴듯하게 틀리기 가장 쉬운 영역이다. 아래를 반드시 지킨다.

- **모든 수치에 출처를 단다.** 규격 번호와 절까지. 예: `TS 38.211 §5.3.1`
- **유도한 값은 계산 과정을 남긴다.** HTML에는 주석으로, 마크다운에는 본문에.
  ```
  <!-- CP = 144·κ·2^-μ·Tc, κ=64, Tc=1/(480e3·4096)
       μ=1 → 144×64×0.5086ns/2 = 2.344 μs -->
  ```
- **확신이 낮은 값은 반드시 표시한다.** HTML은 `<!-- VERIFY: 근거 필요 -->`,
  마크다운은 `⚠️ 미검증`. 표시 없이 넘어가는 것이 가장 나쁜 실패다.
- **추측으로 숫자를 채우지 않는다.** 모르면 "확인 필요"라고 쓴다.
- 검증한 항목은 `refs/3gpp-notes.md`에 옮겨 적고 검증일을 남긴다.
- 유도 수치를 고쳤으면 **`python3 tools/verify-numbers.py`를 돌려 통과시킨 뒤 커밋**한다.
  새로 유도한 값이 생기면 이 스크립트에 검산 항목을 추가한다.

규격 원문: https://portal.3gpp.org (무료)
자주 쓰는 것: TS 38.211(물리채널·변조) · TS 38.213(제어절차) · TS 38.331(RRC)

---

## 2. 설명 방식

- **물리적 직관 → 수식 → 규격 파라미터** 순서로 간다. 수식부터 시작하지 않는다.
- 비유는 하나만 쓰고 끝까지 밀고 간다. 비유를 계속 갈아타면 오히려 헷갈린다.
- 표로 정리 가능한 것은 표로. 문단으로 늘어놓지 않는다.
- 용어는 **영문 원어 + 한글 병기**를 첫 등장 시 한 번. 이후 영문 약어만.
  예: "순환 전치(Cyclic Prefix, CP)" → 이후 "CP"
- 각 자료 끝에는 "그래서 뭐가 남는가"를 3줄 이내로 정리한다.

## 3. 시각 자료

- **인라인 SVG로 직접 그린다.** 이미지 파일이나 외부 다이어그램 도구를 쓰지 않는다.
  텍스트라서 diff가 읽히고, 수치가 바뀌면 그 자리에서 고칠 수 있다.
- **시간 축은 실제 비율로 그린다.** 과장이 불가피하면 캡션에 명시한다.
  ("CP 구간은 가독성을 위해 4배 확대")
- 인터랙션은 **개념을 검증할 수 있을 때만** 넣는다. 슬라이더를 움직였을 때
  독자가 "아, 여기서 깨지는구나"를 발견할 수 있어야 한다. 장식용 애니메이션은 뺀다.
- 모든 SVG에 `<title>`과 `<desc>`를 넣는다.

## 4. 스타일

`assets/base.css` 밖에서 **새로운 색을 정의하지 않는다.** 토큰만 쓴다.

| 토큰 | 의미 | 이 색이 나오면 독자는 이렇게 읽는다 |
|---|---|---|
| `--dl` / `--dl-bg` | 다운링크, 직접파 | 기지국 → 단말 |
| `--ul` / `--ul-bg` | 업링크, 안전 상태 | 단말 → 기지국 |
| `--gp` / `--gp-bg` | 가드, CP, 버려지는 구간 | 데이터가 아님 |
| `--alert` / `--alert-bg` | 간섭, 반사파, 손상 | 문제가 생긴 지점 |

색의 의미는 자료 전체에서 동일해야 한다. 한 자료에서 파랑이 다운링크였다면
다른 자료에서 파랑을 "안전"으로 쓰지 않는다.

- 서체: 제목 `Gothic A1` 900 / 본문 `IBM Plex Sans KR` 300 / 수치·라벨 `IBM Plex Mono`
- 새 자료는 `_template/index.html`을 복사해서 시작한다.

## 5. 새 자료를 추가할 때

1. `_template/index.html`을 `topics/NN-슬러그/index.html`로 복사
2. 상단 `<title>`, eyebrow 번호, 이전/다음 링크 수정
3. **`index.html`(루트) 목차에 항목 추가** — 빠뜨리면 자료가 고아가 된다
4. 인용한 규격 조항을 `refs/3gpp-notes.md`에 누적
5. 커밋 메시지는 `add: 03 순환 전치(CP)` / `fix: CP 길이 표 μ=3 오기 수정` 형식

## 6. 품질 기준

새 자료를 내보내기 전 확인:

- [ ] `python3 tools/verify-numbers.py`가 통과하는가
- [ ] 모든 수치에 출처 또는 계산 과정이 있는가
- [ ] `VERIFY` 표시가 남아 있다면 의도한 것인가
- [ ] 휴대폰 폭(360px)에서 읽히는가 (넓은 SVG는 `.stage--scroll`로 감쌌는가)
- [ ] 키보드 Tab으로 인터랙션에 접근되는가, 포커스 링이 보이는가
- [ ] SVG에 `<title>`/`<desc>`가 있는가
- [ ] 루트 `index.html`에 링크했는가

## 7. 로드맵 (작성 예정)

- 12 PDCCH와 블라인드 복호 — 스케줄링 정보는 어떻게 찾는가
- 13 상향 물리계층 — PUSCH·PUCCH와 DFT-s-OFDM
- 14 채널 코딩 — LDPC와 Polar
- 15 5G 코어와 세션 — PDU Session, QoS Flow

11까지 작성 완료. 목록은 루트 `index.html` 참조.
